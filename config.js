// Leaf & Loom — front-end config.
// Point this at the deployed /subscribe backend (the app.py capture server).
// Leave empty ("") to disable live capture (form shows "temporarily offline").
window.POD_ENDPOINT = "";
