// Leaf & Loom — front-end config.
// Points at the deployed /subscribe backend (the app.py capture server).
// Captured live on a free localhost.run tunnel; swap to a permanent
// free host (Render / PythonAnywhere) without touching any other file.
window.POD_ENDPOINT = "https://ddbd75ed97c6b5.lhr.life/subscribe";
